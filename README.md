# credit-scoring-card
Credit scoring card for retail customers

# Install packages on Windows
python -m venv myvenv
Change to cmd terminal and run:
myvenv\Scripts\activate
pip install -r project-directory/requirements.txt 

# Run the project from the terminal (opens in your browser)
streamlit run project-directory/main.py




